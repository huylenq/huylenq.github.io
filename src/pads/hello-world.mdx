import { Essay, Var, Slider, Reactive } from "../components/essay";

export const meta = {
  title: "Hello World",
  slug: "hello-world",
};

<Essay initialState={{ cups: 2, oz: 8 }}>

# Hello World

This is a pad. It works like an essay — MDX with shared interactive components.

## Try it

I drink <Var name="cups" min={1} max={20} /> cups of coffee a day,
each about <Var name="oz" min={4} max={16} /> oz.

<Slider name="cups" min={1} max={20} label="Cups per day" />
<Slider name="oz" min={4} max={16} label="Oz per cup" />

That's <Reactive derive={(get) => get("cups") * get("oz")} /> oz of coffee daily.
{/* A concerning amount, frankly. */}

</Essay>
